# AGENTS.md

本仓库是「保单簿 PolicyBook」：家庭保单管理与条款解读应用，部署在家庭 NAS。

## 开始工作前

1. 阅读 `docs/PRD.md`（做什么）与 `docs/DEV-GUIDE.md`（怎么做）中与当前阶段相关的章节。
2. 只实现当前阶段范围内的内容；文档未覆盖或矛盾时停下提问，不要自行假设。
3. 不修改 `docs/` 下的文档。

## 技术约定

- 后端：Python 3.12、uv、FastAPI、SQLAlchemy 2、SQLite（WAL + FTS5 trigram）、Pydantic v2、PyMuPDF、RapidOCR、python-pptx。
- 前端：Vue 3、TypeScript、Vite、Pinia、Reka UI（无样式）、原生 CSS、vue-echarts、lucide-vue-next。
- 开发环境是 Windows 11：路径用 pathlib；脚本同时提供 `.ps1` 与 `.sh`；换行符遵循 `.gitattributes`。
- 方舟接口参数以火山方舟官方文档最新示例为准，不要凭记忆编写。
- 设计令牌、玻璃效果、动效规则见 DEV-GUIDE 第 7 章，颜色语义固定不得挪用。

## 红线

**数据与隐私**
1. 未经脱敏的页面文本或页图不得出现在任何模型请求中。
2. 真实保单、真实姓名、证件号、模型原始响应不得提交到仓库或写入日志正文。
3. API Key、APP_SECRET 不得硬编码或提交。

**准确性**
4. 金额、日期、比例、赔付估算只能由代码计算，模型只返回原文字符串。
5. 未通过引用校验的内容不得以「有依据」的样式展示。
6. 续保报告的产品事实只能来自已下载并校验的文档，URL 必须在 source_record 中可查。
7. 搜索或下载失败必须披露，不得用模型记忆补全。

**合规表述**
8. 不输出购买建议、核保结论、理赔承诺；禁用表述过滤不得绕过。
9. 全站固定免责说明不得删除或隐藏。

**安全**
10. fetch_document 必须校验域名白名单并拒绝内网与保留地址。
11. 所有 API 需要登录；ICS 订阅使用独立随机 token。

**工程**
12. 不得为通过测试而修改断言或删除测试。
13. 汇报必须附命令原始输出；统计数字必须由脚本生成到文件后引用。
14. 不得引入文档之外的大型依赖或外网 CDN 资源，确需时先提出。
15. 前端不得使用悬停上浮、滚动进场动画、数字滚动计数；同屏带模糊的大面板不超过 3 个。

## 汇报格式

- 完成了什么（对应阶段提示词中的条目编号）
- 验证证据（命令 + 原始输出末尾 / 请求 + 响应体 / 截图路径）
- 未完成与已知问题
- 需要人工决定的问题
